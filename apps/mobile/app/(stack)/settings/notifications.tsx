/**
 * Notifications settings route — thin entry point.
 */

import React from 'react';
import { NotificationsSettingsScreen } from '@/components/screens/Settings';

export default function NotificationsSettingsRoute(): React.ReactNode {
  return <NotificationsSettingsScreen />;
}
