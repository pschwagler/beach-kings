/**
 * Appearance settings route — thin entry point.
 */

import React from 'react';
import { AppearanceSettingsScreen } from '@/components/screens/Settings';

export default function AppearanceSettingsRoute(): React.ReactNode {
  return <AppearanceSettingsScreen />;
}
