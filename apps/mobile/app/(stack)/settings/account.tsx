/**
 * Account settings route — thin entry point.
 */

import React from 'react';
import { AccountSettingsScreen } from '@/components/screens/Settings';

export default function AccountSettingsRoute(): React.ReactNode {
  return <AccountSettingsScreen />;
}
