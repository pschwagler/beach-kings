/**
 * Privacy settings route — thin entry point.
 */

import React from 'react';
import { PrivacySettingsScreen } from '@/components/screens/Settings';

export default function PrivacySettingsRoute(): React.ReactNode {
  return <PrivacySettingsScreen />;
}
