/**
 * Change password route — thin entry point.
 */

import React from 'react';
import { ChangePasswordScreen } from '@/components/screens/Settings';

export default function ChangePasswordRoute(): React.ReactNode {
  return <ChangePasswordScreen />;
}
