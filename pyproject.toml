# Shared Python tool config (e.g. Ruff). Run: ruff check apps/backend && ruff format --check apps/backend
[tool.ruff]
target-version = "py311"
line-length = 99

[tool.ruff.format]
quote-style = "double"
