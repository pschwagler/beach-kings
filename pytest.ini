[pytest]
asyncio_mode = auto
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_default_fixture_loop_scope = function
addopts = -W ignore::RuntimeWarning --tb=short
filterwarnings =
    ignore::RuntimeWarning
    ignore::sqlalchemy.exc.SAWarning
    ignore:.*coroutine.*was never awaited:RuntimeWarning
    ignore:.*Exception terminating connection:sqlalchemy.pool
    ignore:.*Future exception was never retrieved:asyncio
    ignore:.*got result for unknown protocol state:asyncpg
    ignore:.*Event loop is closed:RuntimeError
    ignore:.*got Future.*attached to a different loop:RuntimeError
